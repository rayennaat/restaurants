import { PageHeader } from "@/components/dashboard/page-header"; import { Card, CardContent } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { formatMoney } from "@/lib/utils"; import { getRecipes } from "@/server/data";
export default async function RecipesPage() { const recipes = await getRecipes(); return <><PageHeader eyebrow="Food cost" title="Recipes & menu margin" description="Standardize portions and see how supplier price changes affect every menu item."/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{recipes.map(r => <Card key={r.id}><CardContent className="pt-5"><div className="flex items-start justify-between"><h2 className="font-black">{r.name}</h2><Badge tone={r.margin >= 55 ? "success" : "warning"}>{r.margin.toFixed(1)}%</Badge></div><div className="mt-6 grid grid-cols-2 gap-3"><div className="rounded-xl bg-neutral-50 p-3"><p className="text-xs text-[var(--muted)]">Recipe cost</p><b>{formatMoney(r.cost)}</b></div><div className="rounded-xl bg-green-50 p-3"><p className="text-xs text-green-700">Selling price</p><b>{formatMoney(r.price)}</b></div></div></CardContent></Card>)}</div></>; }
