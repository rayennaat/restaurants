"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label";
export function OnboardingForm() { const router = useRouter(); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setLoading(true); const data = Object.fromEntries(new FormData(e.currentTarget)); const response = await fetch("/api/onboarding", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); setLoading(false); if (!response.ok) return setError((await response.json()).error ?? "Could not create workspace"); router.push("/dashboard"); router.refresh(); } return <form onSubmit={submit} className="space-y-4"><div><Label>Restaurant or group name</Label><Input name="organizationName" required placeholder="Example: BIG MO"/></div><div><Label>First location</Label><Input name="locationName" required placeholder="Example: La Marsa"/></div><div className="grid grid-cols-2 gap-3"><div><Label>Currency</Label><select name="currency" className="h-11 w-full rounded-xl border bg-white px-3"><option value="TND">TND</option><option value="USD">USD</option></select></div><div><Label>Locale</Label><select name="locale" className="h-11 w-full rounded-xl border bg-white px-3"><option value="fr-TN">French (Tunisia)</option><option value="ar-TN">Arabic (Tunisia)</option><option value="en-US">English (US)</option></select></div></div>{error && <p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Button className="w-full" disabled={loading}>{loading ? "Creating…" : "Create workspace"}</Button></form>; }
