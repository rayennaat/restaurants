"use client";
import { useForm } from "react-hook-form"; import { z } from "zod"; import { zodResolver } from "@hookform/resolvers/zod"; import { toast } from "sonner";
import { submitOrQueue } from "@/lib/offline/db"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label";
const schema = z.object({ ingredientId: z.string().uuid(), quantity: z.coerce.number().positive(), reason: z.enum(["expired", "damaged", "overproduction", "preparation_error", "quality_issue", "other"]), note: z.string().max(500).optional() }); type Values = z.infer<typeof schema>;
export function WasteForm({ ingredients }: { ingredients: { id: string; name: string; unit: string }[] }) { const { register, handleSubmit, reset, formState: { isSubmitting } } = useForm<Values>({ resolver: zodResolver(schema) }); async function submit(values: Values) { const result = await submitOrQueue("/api/waste", values); toast.success(result.queued ? "Waste entry queued offline" : "Waste recorded"); reset(); } return <form onSubmit={handleSubmit(submit)} className="space-y-4"><div><Label>Ingredient</Label><select {...register("ingredientId")} className="h-11 w-full rounded-xl border bg-white px-3" required><option value="">Select ingredient</option>{ingredients.map(i => <option key={i.id} value={i.id}>{i.name} ({i.unit})</option>)}</select></div><div><Label>Quantity</Label><Input type="number" step="0.001" {...register("quantity")}/></div><div><Label>Reason</Label><select {...register("reason")} className="h-11 w-full rounded-xl border bg-white px-3"><option value="expired">Expired</option><option value="damaged">Damaged</option><option value="overproduction">Overproduction</option><option value="preparation_error">Preparation error</option><option value="quality_issue">Quality issue</option><option value="other">Other</option></select></div><div><Label>Note</Label><Input {...register("note")} placeholder="Optional context"/></div><Button className="w-full" disabled={isSubmitting}>Record waste</Button></form>; }
